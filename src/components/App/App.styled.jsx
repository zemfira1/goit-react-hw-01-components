import styled from '@emotion/styled';

export const Container = styled.div`
    width: 520px;
    margin-inline: auto;
    display: flex;
    flex-direction: column;
    gap: 40px;
    color: #010101;
`;